import { SessionStorage } from './storage';
export interface MemoryStoreLike<K, V> {
    get(key: K): V | undefined;
    set(key: K, value: V): this | undefined;
    delete(key: K): boolean;
}
export interface MemoryStorageOptions {
    store: MemoryStoreLike<string, object>;
}
export declare class MemoryStorage implements SessionStorage {
    private store;
    constructor({ store }?: Partial<MemoryStorageOptions>);
    get(key: string): Promise<object | undefined>;
    set(key: string, value: object): Promise<boolean>;
    delete(key: string): Promise<boolean>;
    touch(): Promise<void>;
}
