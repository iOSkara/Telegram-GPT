import * as Interfaces from '../../generated/telegram-interfaces';
import { Structure } from '../../types/interfaces';
import { User } from './user';
/** This object represents a service message about new members invited to a video chat. */
export declare class VideoChatParticipantsInvited implements Structure {
    payload: Interfaces.TelegramVideoChatParticipantsInvited;
    constructor(payload: Interfaces.TelegramVideoChatParticipantsInvited);
    get [Symbol.toStringTag](): string;
    /** New members that were invited to the video chat */
    get users(): User[];
    toJSON(): Interfaces.TelegramVideoChatParticipantsInvited;
}
