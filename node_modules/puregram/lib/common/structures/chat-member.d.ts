import * as Interfaces from '../../generated/telegram-interfaces';
import { Structure } from '../../types/interfaces';
import { User } from './user';
export declare class ChatMember implements Structure {
    payload: Interfaces.TelegramChatMember;
    constructor(payload: Interfaces.TelegramChatMember);
    get [Symbol.toStringTag](): string;
    /** Information about the user */
    get user(): User;
    /** The member's status in the chat */
    get status(): "creator" | "administrator" | "member" | "restricted" | "left" | "kicked";
    /** Owner and administrators only. Custom title for this user */
    get customTitle(): any;
    /** Owner and administrators only. `true`, if the user's presence in the chat is hidden */
    isAnonymous(): any;
    /**
     * Restricted and kicked only.
     * Date when restrictions will be lifted for this user;
     * unix time
     */
    get untilDate(): any;
    /**
     * Administrators only.
     * `true`, if the bot is allowed to edit administrator privileges of that
     * user
     */
    canBeEdited(): any;
    /**
     * Administrators only.
     * `true`, if the administrator can access the chat event log, chat statistics,
     * message statistics in channels, see channel members, see anonymous administrators
     * in supergroups and ignore slow mode.
     * Implied by any other administrator privilege
     */
    canManageChat(): any;
    /**
     * Administrators only.
     * `true`, if the administrator can post in the channel;
     * channels only
     */
    canPostMessages(): any;
    /**
     * Administrators only.
     * `true`, if the administrator can edit messages of other users
     * and can pin messages; channels only
     */
    canEditMessages(): any;
    /**
     * Administrators only.
     * `true`, if the administrator can delete messages of other users
     */
    canDeleteMessages(): any;
    /**
     * Administrators only.
     * `true`, if the administrator can manage video chats
     */
    canManageVideoChats(): any;
    /**
     * Administrators only.
     * `true`, if the administrator can restrict, ban or unban chat members
     */
    canRestrictMembers(): any;
    /**
     * Administrators only.
     * `true`, if the administrator can add new administrators with a subset o
     * their own privileges or demote administrators that he has promoted,
     * directly or indirectly (promoted by administrators that were appointed by
     * the user)
     */
    canPromoteMembers(): any;
    /**
     * Administrators and restricted only.
     * `true`, if the user is allowed to change the chat title,
     * photo and other settings
     */
    canChangeInfo(): any;
    /**
     * Administrators and restricted only.
     * `true`, if the user is allowed to invite new users to the chat
     */
    canInviteUsers(): any;
    /**
     * Administrators and restricted only.
     * `true`, if the user is allowed to pin messages;
     * groups and supergroups only
     */
    canPinMessages(): any;
    /** `true`, if the user is allowed to create, rename, close, and reopen forum topics; supergroups only */
    canManageTopics(): any;
    /**
     * Restricted only.
     * `true`, if the user is a member of the chat at the moment of the request
     */
    isMember(): any;
    /**
     * Restricted only.
     * `true`, if the user is allowed to send text messages,
     * contacts, locations and venues
     */
    canSendMessages(): any;
    /** `true`, if the user is allowed to send audios */
    canSendAudios(): any;
    /** `true`, if the user is allowed to send documents */
    canSendDocuments(): any;
    /** `true`, if the user is allowed to send photos */
    canSendPhotos(): any;
    /** `true`, if the user is allowed to send videos */
    canSendVideos(): any;
    /** `true`, if the user is allowed to send video notes */
    canSendVideoNotes(): any;
    /** `true`, if the user is allowed to send voice notes */
    canSendVoiceNotes(): any;
    /** Restricted only. `true`, if the user is allowed to send polls */
    canSendPolls(): any;
    /**
     * Restricted only.
     * `true`, if the user is allowed to send animations, games,
     * stickers and use inline bots
     */
    canSendOtherMessages(): any;
    /**
     * Restricted only
     * `true`, if the user is allowed to add web page previews to their messages
     */
    canAddWebPagePreviews(): any;
    toJSON(): Interfaces.TelegramChatMember;
}
