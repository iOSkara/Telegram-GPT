import { Message } from '../common/structures';
import * as Interfaces from '../generated/telegram-interfaces';
import { Telegram } from '../telegram';
import { Constructor } from '../types/types';
import { Context } from './context';
import { CloneMixin, NodeMixin, SendMixin, TargetMixin } from './mixins';
interface ChatSharedContextOptions {
    telegram: Telegram;
    update: Interfaces.TelegramUpdate;
    payload: Interfaces.TelegramMessage;
    updateId: number;
}
/** This object contains information about the chat whose identifier was shared with the bot using a `KeyboardButtonRequestChat` button. */
declare class ChatSharedContext extends Context {
    payload: Interfaces.TelegramMessage;
    private event;
    constructor(options: ChatSharedContextOptions);
    /** Identifier of the request */
    get requestId(): number;
    /** Identifier of the shared chat. This number may have more than 32 significant bits and some programming languages may have difficulty/silent defects in interpreting it. But it has at most 52 significant bits, so a 64-bit integer or double-precision float type are safe for storing this identifier. The bot may not have access to the chat and could be unable to use this identifier, unless the chat is already known to the bot by some other means. */
    get sharedChatId(): number;
}
interface ChatSharedContext extends Constructor<ChatSharedContext>, Message, TargetMixin, SendMixin, NodeMixin, CloneMixin<ChatSharedContext, ChatSharedContextOptions> {
}
export { ChatSharedContext };
